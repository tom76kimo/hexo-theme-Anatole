.page-top.animated.fadeInDown
  .nav
    li
      if is_home()
        a.current(href=theme.root + "/")= __('Home')
      else
        a(href=theme.root + "/")= __('Home')
    //- li
    //-   if is_current('about')
    //-     a.current(href=theme.root + "/about")= __('About')
    //-   else
    //-     a(href=theme.root + "/about")= __('About')
    li
      if is_archive()
        a.current(href=theme.root + "/archives")= __('Archive')
      else
        a(href=theme.root + "/archives")= __('Archive')
    li
      if is_current('links')
        a.current(href=theme.root + "/links")= __('Links')
      else
        a(href=theme.root + "/links")= __('Links')
  .information
    .back_btn
      li
        if is_post()
          a.fa.fa-chevron-left(onclick="window.history.go(-1)")
        else
          a.fa.fa-chevron-left(onclick="window.history.go(-1)",style="display:none;")
    .avatar
      img(src=theme.avatar)
